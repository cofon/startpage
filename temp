这是个浏览器起始页项目，使用Vue3开发;
最初创建项目的时候，计划的是npm run build编译成纯本地的项目,双击打开/dist/index.html就可以运行;
数据保存在本地，导入/导出JSON文件，手动维护数据;
获取网站title desc icon数据会有跨域问题，最初的解决方案是创建toos目录，在tools目录写一个python获取数据，数据保存到JSON，手动导入，写好之后感觉不好用，新的决方案是创建一个extension插件目录，写一个插件，使用浏览器的插件环境获取网站的title desc icon数据，插件获取数据后，通过content script与主页面通信，将数据传递给主页面，主页面再保存数据，问题解决了，但是体验不是太好;
现在项目基本完成，感觉本地终究是有太多限制，我在网上搜索之后，知道有一些免费的静态网站托管服务和数据库存储服务，虽然免费版本有一些限制，但是对我来说足够了，因为这这是一个很小的单页面应用，免费额度足够我使用了，所以，现在想把项目改成可以发布到静态网站托管服务上，使用数据库存储数据;
因为之前为了让项目可以本地运行修改了配置文件，为了解决跨域问题，做了很多尝试，修改了很多工具类函数，现在代码一团乱麻，我自己都分不清，想要彻底重构，告诉我改怎么做;
先确定修改之后的使用环境：
1. 项目发布到腾讯edgeone pages这种静态网站托管服务上；
2. 获取网站title desc icon 数据使用腾讯的边缘函数，腾讯提供的有node.js的免费额度(你帮我确认一下，我没有去官网查看)；
3. 数据存储我是在网上搜索，知道有免费的，但是具体哪个没有去找，暂时先使用indexeddb调试，之后换成免费的数据库存储服务；
4. 我没有域名，要使用edgeone的pages提供的域名，或者使用免费的域名；
5. 重构的时候，尽量把一些功能模块独立出来，做成封闭的组件；
6. 重构的时候，是在原来的基础上修改还是重建一个项目；




边缘函数 
免费额度：通常每月 10-100 万次请求（需确认最新政策）
    免费版额度已确认：
        请求次数：300 万次/月（个人版）[7]。
        单次运行限制：64 次 fetch、8 并发、默认 15 秒超时（可设至 300 秒）[5]。
        写函数的时候注意调用，一次for循环的次数限制在64以内
        比如for循环获取网站meta，200个网站分为4次获取，
    如果一次超过64次fetch，这是智能客服给出的回复：
        根据 EdgeOne 边缘函数的运行时限制，单次运行中最多发起 64 次 fetch 请求。若一次性发起 65 次 fetch，具体表现如下：
        请求失败与异常抛出
        超出限制的 fetch 请求（即第 65 次及后续请求）会直接失败，并抛出异常（如 FetchError 或类似错误）[1]。
        错误信息通常包含请求次数超限的提示，例如 "Exceeded maximum fetch count (64)"。
        已发起的请求不受影响
        前 64 次 fetch 请求会正常执行，不受后续失败请求的影响[1]。
        无特定错误码返回
        异常类型为运行时错误，而非 HTTP 错误码（如 4xx/5xx）。需通过代码捕获异常处理[1]。
        建议：
        通过分批次或并发控制（如 Promise.all 限制并发数）避免触发限制[1]。
        若业务必须超过 64 次请求，需拆分逻辑到多个边缘函数调用。
数据库存储 
    现在暂时使用indexeddb，因为免费的云数据库需要测试速度之后才能确定
    先把项目搞好，之后有时间在找几个云数据库测试效果
域名方案
    先使用腾讯edgeone提供的网址，域名需要备案，费时间，暂时不使用
目录结构
    工具类没有必要分的太细
    按照文件名分类就好了，比如：utils/addWebsite.js
    添加网站用到的函数全部放在这个文件