-------------------------------------------------------------------------------
D:\vue\startpage\src\utils\website\websiteUtils.js

验证URL是否有效，返回
export function isValidUrl(urlString)

验证URL是否有效，内部调用isValidUrl，兼容旧版本的简单布尔返回值
export function isValidUrlSimple(url)

返回主机名，输入 https://www.baidu.com， 返回 www.baidu.com
export function extractDomain(url)

从域名中提取网站名，输入 www.baidu.com，返回 baidu
export function extractSiteName(domain)

从URL中提取网站名，输入 https://wwww.baidu.com/path/, 返回 baidu
export function extractSiteNameFromUrl(url)

输入name，返回<svg>xxx</svg>，注意：不是svg base64
输入URL也可以，有从URL提取name的逻辑
有重复代码只后要使用函数替代
export function generateDefaultIcon(name)

使用CORS代理获取网站的title description，现在已经用不着了，之后要删除
export async function fetchWebsiteInfo(url)

验证是否有效图片，输入base64,返回bool
function validateIcon(iconData)

创建默认的网站数据，默认字段填充数据，两个icon字段有问题，以后要处理，看哪个地方调用了，要查看有没有不报错的错误
export function createDefaultWebsiteData(url)

从主机名中提取根域名，输入 www.baidu.com 输出 baidu.com，复合后缀无法处理，输入 www.abc.co.uk，输出 .co.uk
export function extractRootDomain(hostname)
-------------------------------------------------------------------------------
D:\vue\startpage\src\utils\website\websiteNormalizer.js

创建标准网站对象，有些字段已经删除了，需要处理
export function createWebsiteObject(data = {})

从响应式对象创建普通对象（用于数据库操作），输入响应式对象，返回普通对象，有已删除旧字段，要删掉
export function normalizeWebsiteForDB(website)

批量标准化网站对象（用于数据库操作），内部是 websites.map(website => normalizeWebsiteForDB(website))
export function normalizeWebsitesForDB(websites)

创建用于更新的网站对象（只包含需要更新的字段）
export function createUpdateObject(website, updates = {})
-------------------------------------------------------------------------------
D:\vue\startpage\src\utils\website\websiteImportUtils.js

验证网站数据是否完整，必须有值的字段是否空值，需要修改，现在有些字段不是必须的了
export function validateWebsite(website)

填充网站的默认字段
export function fillDefaultFields(website)

批量处理导入的网站数据，有效(完整)/无效网站分别放在有效/无效数组
export function processImportWebsites(websites)

处理单个网站的导入
export function importSingleWebsite(website, websitesStore, onComplete)

将网站添加到store(状态管理)
function addWebsiteToStore(website, websitesStore, onComplete)

更新存储(状态管理)中的网站数据
function updateWebsiteInStore(website, websitesStore, onComplete)
-------------------------------------------------------------------------------
D:\vue\startpage\src\services\importService.js

标准化URL，输入 https://www.baidu.com/，输出 https://www.baidu.com
function normalizeImportUrl(url)

导入数据的主入口
export async function importData(jsonData, options = {})

验证导入数据格式，websites | URLs | error
export function validateImportData(data)

判断导入类型，'websites' | 'urls' | 'unknown'
export function getImportType(data)

检查网站数据完整性（仅针对插件可补全的字段），判断是否需要 边缘函数API获取网站数据
export function isWebsiteComplete(website)

分离完整和不完整的网站
export function separateWebsites(websites)

批量补全网站元数据（只补全插件可提供的字段），现在是 边缘函数补全
export async function enrichWebsites(websites, config, progressCallback)

标准化网站数据，内部：websites.map(website => normalizeWebsiteData(website))
export function normalizeWebsites(websites)

批量导入网站到数据库，数据库操作
export async function importWebsitesToDB(websites, config, monitor, existingUrlsMap = null)

导入 websites 数据，另一个是导入urls
async function importWebsites(websites, config, monitor)

获取缺失的字段列表，针对title desc icon
function getMissingFields(website)

导入 urls 数据，另一个是导入websites，之后要把urls转为websites，合并操作
async function importUrls(urls, config, monitor)
-------------------------------------------------------------------------------
D:\vue\startpage\src\services\websiteMetadataService.js

这个是获取网站title desc icon的services

const LOCAL_API_BASE_URL = import.meta.env.VITE_LOCAL_API_URL || 'http://localhost:3000';

从本地 API 获取网站元数据，local API，本地nodejs启动的服务，返回 metadata{title desc iconData}
export async function fetchMetadataFromLocalApi(url) 

通用的元数据获取函数, 内部调用 fetchMetadataFromLocalApi，以后要换成调用 edgeone边缘函数
export async function fetchMetadata(url, options = {})

验证网站数据，验证可输入字段，不知道在哪用的
export function validateWebsite(data) 

标准化网站数据，不知道在哪用的
export function normalizeWebsiteData(data)

编码 SVG 为 Base64
function encodeSvg(svg)

URL 规范化处理
function normalizeUrl(url)

检查 URL 是否已存在于数据库中
export function checkUrlExists(url, allWebsites)

批量补全网站元数据
export async function batchEnrichMetadata(websites, progressCallback, options = {})
-------------------------------------------------------------------------------