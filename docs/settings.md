我想重新设计设置面板，帮我完善方案：

设置面板
1. 设置面板分为两个部分，上方和下方；
2. 上方一行放置内容如下：“设置”，主题icon按钮，搜索icon按钮，搜索结果模式切换的switch按钮，添加网站icon按钮，导入导出iicon按钮，退出按钮，点击 主题 搜索 添加网站 的 icon按钮，在下方显示对应设置面板；我不确定一行能不能放下，如果放不下，可以换行，或者设置一个other按钮
2. 下方显示上方按钮对应的设置面板，比如点击主题按钮，下方显示主题设置面板，点击搜索按钮，下方显示搜索设置面板，点击添加网站按钮，下方显示添加网站设置面板，
3. 模式切换的Switch按钮不需要显示在下方面板中，直接执行，导入导出按钮也不需要显示在下方面板中，直接执行；执行后显示通知条显示执行结果，
4. 现在不确定上方有没有other按钮，如果有，把不常用的设置项放在other对应的设置面板中；

主题设置面板：
1. 从上往下依次是：主题选择列表，当前选中主题的可设置颜色列表，主题名称输入框，按钮；
2. 主题颜色列表：一行带有颜色的圆，圆的颜色是主题颜色，selected theme的圆有一个带颜色的边框，用来与其他 unselected theme 的圆进行区分，我不知道具体怎么做，高亮还是别的什么，你帮忙设计；
3. (selected theme的)可设置颜色列表：primary ， text， background 等颜色的列表，显示方式为，每个颜色一行，颜色名称，颜色块，颜色值，点击颜色方块显示调色板选择颜色，颜色值即时更新，点击确认按钮修改颜色并把数据更新到数据库中，并刷新页面；以primary为例，显示为：primary，颜色方块，颜色值(可以是一个输入框，以便手动输入颜色值)；我不知道设计一个网页的主题需要哪些颜色，我只是在某个地方看到过primary textMain textSecondary textDisabled bgPage bgCard borderBase shadowLight等词汇，但是我不知道这些是做什么的，你帮我根据项目实际情况进行设计；注意：有数据库操作，现在数据库没有存储主题的设计，所以，数据库也要重新设计；
4. 主题名称输入框：一个输入框，自动填入selected theme的名称；
4. 按钮，默认不可点击，修改数据后，确认按钮可点击，如果修改了主题名称，按钮的文字显示为添加主题，如果修改了主题颜色，确认按钮的文字显示为修改主题，默认显示为添加主题；

搜索设置面板：
1. 上方显示搜索引擎列表，每个搜索引擎一行，图标，名称，搜索引擎模板(百度为例：https://www.baidu.com/s?wd={query})， 编辑icon按钮(点击后数据填充到下方的编辑区)，删除icon按钮；
2. 下方显示搜索引擎编辑面板，第一行：上传图标按钮，上传图标的预览，图标的string输入框，第二行：搜索引擎名称输入框，第三行：搜索引擎模板输入框，第四行：icon颜色方块和颜色值输入框，第五行：确认按钮，取消按钮，默认不可点击，添加搜索引擎有条件判断，符合条件后，确认按钮可点击；
3. 编辑区既可以编辑现有的搜索引擎，也可以添加新的搜索引擎，具体如何判断是编辑现有的还是新增的，需要你帮我设计，搜索引擎名字不是唯一的，无法作为判断标准，搜索引擎模板不知道是不是唯一，你帮我设计吧，总之，是编辑还是新增必须有一个判断标准，不然数据库会出问题
4. 现在数据库中没有搜索引擎的存储方案，所以，数据库也要重新设计；

搜索结果显示模式切换按钮(switch)：
1. 默认为列表模式，点击切换为网格模式，数据库存储一个搜索结果模式，默认为列表模式，切换后保存数据库，下次打开时读取数据库，显示对应的模式；
2. 切换模式之后立刻关闭设置面板；
3. Switch的样式：
    - 一个长方形 A，宽度100, 背景色灰色，四角都是圆形；
    - 一个长方形 B，宽度60, 背景色蓝色，四角都是圆形，有文字：G L，文字G代表grid，文字L代表list；
    - B 在 A 的上方，作为滑块使用，当 B 在 A 的左边时，代表grid模式，B的文字显示G，当 B 在 A 的右边时，代表list模式，B的文字显示L；
    - 上边的宽度 颜色 文字等样式只是为了方便描述，实际生成代码时根据实际需要调整；

添加网站设置面板：
1. 根据website数据表设计
    website
        {
        id: number, // 自增ID (主键)
        name: string, // 网站名称
        url: string, // 网站链接
        description: string, // 网站描述
        icon: string, // 图标URL（自动获取，失败则使用程序生成）
        tags: Array, // 标签数组 [tag1, tag2, ...]
        isMarked: boolean, // 是否为已标记网站
        markOrder: number, // 常用网站排序（仅当isMarked为true时有效）
        visitCount: number, // 访问次数统计
        lastVisited: Date, // 最近访问时间
        createdAt: Date, // 创建时间
        updatedAt: Date, // 更新时间
        isActive: boolean // 是否激活状态（用于软删除）
        isHidden: boolean // 是否隐藏状态（用于）
        }
2. 默认值设置：
    - id自增不需要显示
    - isMarked默认为false
    - markOrder默认为0
    - visitCount默认为0
    - isActive默认为true
    - isHidden默认为false
3. tags输入框获取焦点时显示提示框，提示框显示标签列表，点击标签时添加到输入框中，多个标签用逗号分隔；

导入导出按钮：
这个不需要设置面板，直接执行
